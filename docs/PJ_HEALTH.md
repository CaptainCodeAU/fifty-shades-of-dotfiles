# pj-health: is the pj framework wired on this machine?

`pj-health` is the on-demand check ruled in D-20260920-09 (c). It runs from any folder,
finishes in about a second, writes nothing, and prints one row per check:

```
STATE         name               detail
```

Four states, and the fourth is the point:

| State          | Means                                                                                             |
| -------------- | ------------------------------------------------------------------------------------------------- |
| `PASS`         | measured, wired as expected                                                                       |
| `WARN`         | measured, not a wiring fault (a pending hand edit to the items view, a fat first turn)            |
| `FAIL`         | measured, wired wrong. The detail ends with the command that fixes it. `pj-health` never runs it. |
| `NOT MEASURED` | the check could not reach its target, and the detail says why. Never a pass.                      |

Exit 0 with no FAIL, 1 with any FAIL, 2 when it refused to run (bad flag, no `jq`).
The shape is borrowed from a sibling project's session-start battery: a grader that
cannot reach its box is reported as unreachable, never as an outage and never as green,
because the two used to be misread for each other.

## Flags

| Flag            | Effect                                                                                                                                                                                                     |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| (none)          | machine checks, the project checks for the repo you stand in, the evidence checks from that repo's newest interactive pj transcript                                                                        |
| `--full`        | also run every framework selftest and quote each one's own pass line                                                                                                                                       |
| `--live`        | also launch ONE headless `pj -p` probe (an API call, haiku) and read its transcript                                                                                                                        |
| `--quiet`       | print only FAIL and NOT MEASURED rows, plus the summary                                                                                                                                                    |
| `--json`        | one object: `project`, `rows[]`, `summary`, `exit`                                                                                                                                                         |
| `--profile <n>` | grade the run as profile `<n>`: its declared `config_dir` decides where transcripts are looked for. Defaults to `$PJ_PROFILE`, which `pj` exports, then to `default`. See [PJ_PROFILES.md](PJ_PROFILES.md) |
| `--selftest`    | prove every arm against fixtures (`pj-health-selftest`, 232 arms)                                                                                                                                          |

`/pj:health` inside a pj session runs it and pastes the rows.

## The rows

Machine level, always:

| Row                | PASS when                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | FAIL when                                                                                                                                                                                                                                                                                                                                                                                                    | NOT MEASURED when                                                                                                                                                                                                                                                                                                       |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `launcher`         | a fresh `zsh -ic` resolves `pj` to a command (the stowed `~/.local/bin/pj` script, F3), no `alias pj` exists, and `pj --dry-run` carries `--setting-sources project,local`, the project settings file, the `pj-prompt-file --path` cache as the appended prompt file and the five `--plugin-dir`s                                                                                                                                                                                                                                                                                                                                      | `pj` is an alias or a function (an alias shadows the script and goes stale: that is the fault F3 removed), the script is missing, the dry run refuses, or any piece is missing (named)                                                                                                                                                                                                                       | the fresh shell printed nothing for `whence -w pj`                                                                                                                                                                                                                                                                      |
| `output-style`     | the project settings file sets `pj-voice:pj-voice` and the style file exists                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | either is wrong                                                                                                                                                                                                                                                                                                                                                                                              | the settings file is unreadable                                                                                                                                                                                                                                                                                         |
| `plugin-dirs`      | each plugin dir holds a plugin manifest, a skill or an output style                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | one is missing or empty                                                                                                                                                                                                                                                                                                                                                                                      |                                                                                                                                                                                                                                                                                                                         |
| `prompt-file`      | `pj-prompt-file` builds and its cache equals its sources joined by one newline, byte for byte                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | it differs or the tool fails                                                                                                                                                                                                                                                                                                                                                                                 | a source is missing, or the tool fell back to a source because the cache was not writable (a sandbox)                                                                                                                                                                                                                   |
| `rules-budget`     | the machine-wide rules file is within its TOKEN budget, default 2,200, override with `PJ_RULES_TOKEN_BUDGET`. Bytes divided by 2.45, the chars-per-token ratio measured on this text at P9: removing `RULES.md` from a real pj launch moved the first turn from 62,162 to 60,561, so 3,930 chars cost 1,601 tokens. **This was a 60-LINE cap until 2026-09-22 and the line count never bound.** At P5.4 the file sat at 31 lines of its 60 while six approved pinned lines were cut from it, and they were cut by a 1.5k `/context` reading the cap could not see. A line cap cannot tell a 40-character line from a 400-character one | over budget (the row names the byte count, the line count and the fix)                                                                                                                                                                                                                                                                                                                                       | absent                                                                                                                                                                                                                                                                                                                  |
| `settings-render`  | `claude-project-settings-render --check` reports current                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | drift                                                                                                                                                                                                                                                                                                                                                                                                        | tool absent                                                                                                                                                                                                                                                                                                             |
| `hooks-sync`       | every manifest entry targeted at `project` is registered in the project settings file (same event, matcher and command triple the sync tool uses)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | one is not                                                                                                                                                                                                                                                                                                                                                                                                   | manifest or settings unreadable. The sync tool refuses `--check` inside a sandbox, so the comparison is done directly and the refusal is noted                                                                                                                                                                          |
| `hook-orphans`     | every registered command resolves to an executable                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | an orphan (named, with the `jq` line that would remove it, printed and not run: the sync tools are add-only, so this is the only way an orphan is ever seen)                                                                                                                                                                                                                                                 | settings unreadable                                                                                                                                                                                                                                                                                                     |
| `hook-single-fire` | no command sits in both the project settings file and the repo's own `.claude/settings.json`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | one does (named)                                                                                                                                                                                                                                                                                                                                                                                             | outside a repo, or the repo has no `.claude/settings.json`                                                                                                                                                                                                                                                              |
| `stow-links`       | every framework tool, hook script and pj plugin file under `~` is a link into the dotfiles checkout                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | a link is dangling, absent, or a real file                                                                                                                                                                                                                                                                                                                                                                   | no dotfiles checkout found                                                                                                                                                                                                                                                                                              |
| `bin-strays`       | every entry in `~/.local/bin` is either a stow link into the dotfiles checkout or a tool-install link (uv, bun, claude link their own binaries in here). Prints the denominator on a clean run: `N entries: N stow, N tool-install, 0 in no repo`                                                                                                                                                                                                                                                                                                                                                                                      | **never FAILs.** WARNs and names any entry that is a real file, or a link pointing somewhere other than the dotfiles checkout or a tool-install directory. A real file there is not a fault -- it is often another project's, parked here -- but it is versioned NOWHERE unless some repo claims it, and nothing else on the box reports that. Fix: adopt it into a repo, or record it as deliberately local | `~/.local/bin` does not exist                                                                                                                                                                                                                                                                                           |
| `machine-file`     | `~/.config/pj/machine` holds a `name:` and a `letter:` (one capital A-Z), and no retired `range:` line. WARN when a leftover `range:` sits beside a good letter. Since P8a (2026-09-21) every W-/D- ID carries that letter: `W-20260921-A07`                                                                                                                                                                                                                                                                                                                                                                                           | absent, letterless, a bad letter, or still on the retired `range:` scheme -- `pj-id` REFUSES to allocate from any of those, so `open-items add` and `decided add` are dead on that box                                                                                                                                                                                                                       | never: there is no default letter, so there is nothing this row cannot measure                                                                                                                                                                                                                                          |
| `pj-global`        | the machine-wide home has its rules file, decisions folder and notes index, and `decided --list` from outside any repo returns the machine-wide count                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | a file missing, or `decided` refuses                                                                                                                                                                                                                                                                                                                                                                         | `decided` not on PATH                                                                                                                                                                                                                                                                                                   |
| `no-wrap-up`       | always: how many projects have sessions that ended without wrap-up, and how many records                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | never                                                                                                                                                                                                                                                                                                                                                                                                        | the flag folder is unreadable                                                                                                                                                                                                                                                                                           |
| `selftests`        | every framework selftest exists; under `--full`, every one exits 0 and its pass line is quoted                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | one missing, or one fails                                                                                                                                                                                                                                                                                                                                                                                    |                                                                                                                                                                                                                                                                                                                         |
| `profile`          | the profile named by `--profile` (or `$PJ_PROFILE`, which `pj` exports) exists and its declared `config_dir` is on disk. The detail names the profile, its config dir and the transcripts dir that follows from it                                                                                                                                                                                                                                                                                                                                                                                                                     | the named profile does not exist (the detail lists the ones that do)                                                                                                                                                                                                                                                                                                                                         | **WARN**, never FAIL, when the declared `config_dir` does not exist yet: that is the normal state of a freshly declared profile, and a fresh config dir has no credentials and no folder trust, so the detail names the `/login` and the trust dialog only a human can clear                                            | no profiles dir at all, which is a supported state: `pj` then uses its built-in argv                                                                                                                                                                                             |
| `profile-siblings` | every profile's `config_dir` is a sibling under `~`, one level down, like `~/.claude`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | one `config_dir` sits INSIDE another's -- `~/.claude/<x>` would put a profile's transcripts inside the RECORDS repo, where dot-claude would try to track them                                                                                                                                                                                                                                                | **WARN** for a config dir deeper than one level under `~`, and separately for one not under `~` at all (said in those words: it is not deep, it is elsewhere). Neither is measured to break anything, and a FAIL on an unmeasured hazard is a false alarm                                                               | no profiles dir                                                                                                                                                                                                                                                                  |
| `profile-registry` | every profile with a config dir of its own has `<config_dir>/sessions` as a symlink to `~/.claude/sessions`, the one folder `ListAgents` reads. That shared registry is what lets a `pj` session and a `c2`/scratch session see each other and `SendMessage` by name, in both directions (F5c, gate Q1(a))                                                                                                                                                                                                                                                                                                                             | `~/.claude/sessions` is ITSELF a symlink. It is the target every profile links TO, so default-profile sessions are then registering where nothing reads them -- the mechanism inverted. This is the only FAIL here                                                                                                                                                                                           | **WARN** for every other miss, because none of them is broken: a config dir that does not exist yet, no `sessions` entry yet, a real `sessions` directory (pj adopts an empty one at the next launch), a DANGLING link (pj repairs it), or a link deliberately pointing somewhere else (pj leaves it alone and says so) | no profile declares a config dir of its own, so there is nothing to share. That is the default state on a fresh machine, not a fault                                                                                                                                             |
| `stale-workspaces` | every Herdr workspace that NAMES a worktree points at a `checkout_path` that is still on disk. Added F5d for `W-20260922-A04`: `hwt` creates a Herdr workspace beside the git worktree and `c2 done` never closed it, so every discarded scratch topic left a dead sidebar entry. `c2` closes its own since F5d, so a new one here was made some other way                                                                                                                                                                                                                                                                             | never FAILs -- a stale sidebar entry costs nothing but attention                                                                                                                                                                                                                                                                                                                                             | **WARN**, naming each workspace id and the path that is gone, with the `herdr workspace close <id>` to clear it                                                                                                                                                                                                         | `herdr` or `jq` is not on PATH, the server answered nothing (the Bash sandbox blocks its socket), or it listed no workspaces. The PASS text states BOTH denominators -- how many named a worktree, of how many examined -- so "0 stale" and "0 examined" can never read the same |
| `records-pinned`   | `open-items --where` returns the SAME path with and without a foreign `CLAUDE_CONFIG_DIR`, and that path is under `~/.claude`. A differential, not a source read: it measures what the tool does, not what its code says today                                                                                                                                                                                                                                                                                                                                                                                                         | the answer MOVES with `CLAUDE_CONFIG_DIR`, or resolves outside `~/.claude`. Either means the drawer forks per profile, and a scratch session would file items into a register nobody else reads                                                                                                                                                                                                              |                                                                                                                                                                                                                                                                                                                         | outside a repo, or `open-items --where` printed nothing, so the control never hit and the comparison proves nothing. On a seamed run the root half is skipped and said so: a fixture legitimately points the drawer elsewhere                                                    |
| `launch-log`       | none of the last 20 lines of `~/.local/state/pj/launches.log` (written by the `pj-launch-check` SessionStart hook, F3) is `result=stale:`; unmeasured lines are counted, not failed                                                                                                                                                                                                                                                                                                                                                                                                                                                    | never: a stale launch is history, not wiring. **WARN** names each stale launch as `tty date time (reasons)`                                                                                                                                                                                                                                                                                                  | the log does not exist yet (the first pj launch after F3 writes it) or has no `result=` lines                                                                                                                                                                                                                           |
| `stale-shells`     | no interactive `zsh` of yours (a real tty, parent not pid 1, no `-c`) started before `~/.zshrc`'s real mtime (`stat -L`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | never: an old shell is a warning about future launches. **WARN** lists `tty pid since date`; fix is to close the pane or open a new one. `exec zsh` keeps the process start time, so an exec'd pane stays listed until reopened. Every `.zshrc` edit makes every open shell stale by this measure, which is correct                                                                                          | `ps` returned nothing (a sandbox refuses it) or `~/.zshrc` cannot be stat'ed                                                                                                                                                                                                                                            |

Project level, inside a repo:

| Row              | PASS                                                                                                                                                                    | FAIL                                                                                                 | WARN                                                                                                                                                                                                                                                                                        | NOT MEASURED                                        |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| `pj-homes`       | the declaration parses, `records` is `repo` or `private`, every declared store exists                                                                                   | unknown key, bad value, or a declared store missing                                                  | a marker the tools do not resolve                                                                                                                                                                                                                                                           | outside a repo, or no declaration (defaults in use) |
| `drawer`         | the items folder exists and the generated view matches its stamp                                                                                                        | `open-items --where` prints nothing                                                                  | the view differs from its stamp (a hand edit; `open-items regen` rescues it), or the legacy single-file layout                                                                                                                                                                              | no drawer yet (`open-items init`)                   |
| `decided-store`  | `decided --homes` finds the project store                                                                                                                               | the store is declared in a format `decided` cannot read                                              | outside a repo, or the store is not present                                                                                                                                                                                                                                                 |
| `commit-trailer` | the newest commit in this repo and in dot-claude either carries a non-empty `C-Sess-Id` or carries no `C-Sess-Id` key at all (a terminal commit, or `trailers.disable`) | never: a past commit cannot be repaired without a history rewrite, so a FAIL here would be permanent | the newest commit carries the `C-Sess-Id` key with an EMPTY value. The stamp fired and the id was missing, so the commit LOOKS attributed to anything that only checks the key is present. Fix: `audit-chain-selftest`, then check `_audit-chain` still reads `CLAUDE_CODE_SESSION_ID` (F4) | no named repo is reachable                          |

Evidence, read from the newest interactive pj transcript for the repo (the one whose
output style is pj-voice; headless `-p` probes are skipped):

| Row                 | PASS                                                             | FAIL                                                                                                                                                                                                     | NOT MEASURED                    |
| ------------------- | ---------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- |
| `evidence-card`     | the start card hook printed exactly once                         | 0 or more                                                                                                                                                                                                | no transcript                   |
| `evidence-ci-watch` | the CI watch hook printed exactly once                           | 0 or more                                                                                                                                                                                                | no transcript                   |
| `evidence-rules`    | the machine-wide rules file's header is in the prompt snapshot   | absent (before F3: a shell opened before the alias changed launched without it; since F3 `pj` is a script and `pj-launch-check` shouts at start, so a FAIL here means a launch that bypassed the script) | no snapshot record              |
| `evidence-plugins`  | the four skills and the output style are named in the transcript | one absent                                                                                                                                                                                               | no transcript                   |
| `evidence-tokens`   | the first API call's input tokens are within budget              | never: over budget is WARN, a long first message is not a wiring fault                                                                                                                                   | no usage record, or no baseline |

The budget: for the dotfiles repo a header constant of 70,000 plus 15 percent (from the
P5.4 measurement of a one-word first turn); for any other repo the median of its last five
interactive pj transcripts plus 15 percent, needing at least three. The script header
carries the numbers.

Live, `--live` only:

| Row          | PASS                                                                                                                          | FAIL                                               | NOT MEASURED                                                    |
| ------------ | ----------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- | --------------------------------------------------------------- |
| `live-probe` | the probe's transcript shows the start card once and the dummy `${DUMMY_TOKEN:-no}` line was denied by the secret-probe guard | the dummy line ran, or the card did not print once | without the flag; outside a repo; the launcher returned nothing |

The probe never carries a real secret. Its token figure is printed labelled as an
under-read: headless runs report fewer tokens than an interactive session for the same
prompt (measured in P5.4).

## Reading it

- A FAIL row is a wiring fault. Run the command in its detail from a terminal, then
  `pj-health` again.
- A NOT MEASURED row is not a pass. Read the reason. Most are expected in a given place:
  outside a repo the project rows say so; a project without a drawer says so.
- `--quiet` in a script: the exit code is the answer, the rows are the reasons.
- Inside a Claude session's sandbox two rows go NOT MEASURED on their own (`prompt-file`,
  because the cache is not writable there; and the sync tool's own `--check` refuses, so
  `hooks-sync` says it compared directly). Run from a terminal for the full picture.

## Test seams

The selftest drives every arm through environment overrides, one per real path. They are
listed in the script header. Nothing else should set them.
